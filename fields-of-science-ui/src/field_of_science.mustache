<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <title>{{field_name}}</title>
    <style>
        .container {
            margin: auto;
            width: 90%;
        }

        th {
            padding: 20px 15px;
            text-align: left;
            font-weight: 500;
            font-size: 12px;
            text-transform: uppercase;
            border-bottom: solid 1px black;
        }

        td {
            padding: 15px;
            text-align: left;
            vertical-align: middle;
            font-weight: 300;
            font-size: 12px;
            border-bottom: solid 1px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
<div class="container">
    <h2>
        Here are our clients who are currently involved in the field of {{field_name}}
    </h2>
    <table>
        <thead>
        <tr>
            <th>Title</th>
            <th>Type</th>
            <th>University</th>
            <th>Principal Investigator</th>
            <th>Abstract</th>
        </tr>
        </thead>
        <tbody>
        {{#collaborators}}
            <tr>
                <td><a href="{{url}}">{{title}}</a></td>
                <td>{{type}}</td>
                <td>{{#university}}
                    <a href="{{url}}">{{name}}</a>
                {{/university}} </td>
                <td>{{#principal_investigator}}
                    {{first_name}} {{middle_name}} {{last_name}}
                {{/principal_investigator}} </td>
                <td>{{abstract}}</td>
            </tr>
        {{/collaborators}}
        </tbody>
    </table>
</div>
</body>
</html>
